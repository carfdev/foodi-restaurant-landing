---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a href={href} class={Astro.url.pathname === href ? 'active-link' : 'hover:text-primary/90'}
  >{label}</a
>
