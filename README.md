# Astro + Vue + Tailwind + Prettier Template

Esta es una plantilla minimalista y lista para usar configurada con las mejores herramientas para desarrollo web moderno. Ideal para iniciar nuevos proyectos rápidamente sin perder tiempo en configuraciones iniciales.

## 🛠 Tecnologías Incluidas

- **[Astro 5](https://astro.build/)**: Framework web optimizado para velocidad.
- **[Vue 3](https://vuejs.org/)**: Framework progresivo para interfaces de usuario interactivas.
- **[Tailwind CSS 4](https://tailwindcss.com/)**: Framework CSS de utilidad primero (usando la versión Vite más reciente).
- **[Prettier](https://prettier.io/)**: Formateador de código configurado con plugins para Astro y Tailwind.

## 🚀 Comandos Disponibles

Todos los comandos se ejecutan desde la raíz del proyecto:

| Comando           | Acción                                                               |
| :---------------- | :------------------------------------------------------------------- |
| `npm install`     | Instala las dependencias del proyecto.                               |
| `npm run dev`     | Inicia el servidor de desarrollo local en `localhost:4321`.          |
| `npm run build`   | Compila tu sitio para producción en `./dist/`.                       |
| `npm run preview` | Vista previa de tu compilación localmente antes de desplegar.        |
| `npm run format`  | Formatea todo el código (Astro, Vue, TS, CSS, etc.) usando Prettier. |

## 📦 Estructura del Proyecto

```text
/
├── public/
│   └── favicon.svg
├── src/
│   ├── assets/
│   ├── components/
│   ├── layouts/
│   └── pages/
└── package.json
```

## � Uso

1. **Instalar dependencias:**

   ```sh
   npm install
   ```

2. **Iniciar servidor de desarrollo:**

   ```sh
   npm run dev
   ```

3. **Formatear código:**
   ```sh
   npm run format
   ```

¡Disfruta construyendo con esta configuración optimizada!
